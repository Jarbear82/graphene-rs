[package]
name = "graphene_core"
version = "0.1.0"
edition = "2021"

[features]
default = ["serde"]
serde = ["dep:serde", "dep:serde_json"]

[dependencies]
slotmap = { version = "1.0", features = ["serde"] }
bitvec = "1.0"
smallvec = "1.11"
bitflags = "2.4"
serde = { version = "1.0", features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }

[dev-dependencies]
graphene_algo = { path = "../graphene_algo" }
